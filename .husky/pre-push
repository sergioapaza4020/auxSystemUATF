echo "Running lint..."
pnpm turbo run lint
if [ $? -ne 0 ]; then
  echo "Lint failed. Push blocked."
  exit 1
fi

echo "Running tests..."
pnpm turbo run test
if [ $? -ne 0 ]; then
  echo "Tests failed. Push blocked."
  exit 1
fi

echo "Push allowed"